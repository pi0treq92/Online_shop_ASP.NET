@using twoja_manufaktura.Infrastructure
@model IEnumerable<twoja_manufaktura.Models.Kategoria>

    <div class="col-lg-3">

        <h1 class="my-4"><img src="/Content/Images/logo.jpg" /></h1>
        <div class="list-group">
            @foreach (var kategoria in Model)
            {
                <a href="@Url.Action("List", "Store", new {genrename = kategoria.Name.ToLower() })"
                   style="background-image: url(@Url.GenreIconPath(kategoria.IconFileName));background-position:right;background-repeat:no-repeat" class="list-group-item">@kategoria.Name</a>

            }

        </div>

    </div>